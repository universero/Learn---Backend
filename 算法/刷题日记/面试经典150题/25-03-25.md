## 98. 验证二叉搜索树

### 题目描述

给你一个二叉树的根节点 root ，判断其是否是一个有效的二叉搜索树。

有效 二叉搜索树定义如下：

节点的左子树只包含 小于 当前节点的数。
节点的右子树只包含 大于 当前节点的数。
所有左子树和右子树自身必须也是二叉搜索树。

### 题解

```go
func isValidBST(root *TreeNode) bool {
	in := make([]int, 0)
	var inorder func(root *TreeNode)
	inorder = func(root *TreeNode) {
		if root == nil {
			return
		}
		inorder(root.Left)
		in = append(in, root.Val)
		inorder(root.Right)
	}
	inorder(root)
	for i := 0; i < len(in)-1; i++ {
		if in[i+1] <= in[i] {
			return false
		}
	}
	return true
}
```

### 问题记录

### 知识点

## 200. 岛屿数量

### 题目描述

给你一个由 '1'（陆地）和 '0'（水）组成的的二维网格，请你计算网格中岛屿的数量。

岛屿总是被水包围，并且每座岛屿只能由水平方向和/或竖直方向上相邻的陆地连接形成。

此外，你可以假设该网格的四条边均被水包围。

### 题解

```go
func numIslands(grid [][]byte) int {
	ans := 0
	m := len(grid)
	n := len(grid[0])
	xx := []int{-1, 0, 1, 0}
	yy := []int{0, -1, 0, 1}
	var dfs func(x, y int)
	dfs = func(x, y int) {
		for i := 0; i < 4; i++ {
			a := xx[i] + x
			b := yy[i] + y
			if 0 <= a && a < m && 0 <= b && b < n && grid[a][b] == '1' {
				grid[a][b] = '0'
				dfs(a, b)
			}
		}
	}
	for i := 0; i < m; i++ {
		for j := 0; j < n; j++ {
			if grid[i][j] == '1' {
				ans++
				dfs(i, j)
			}
		}
	}
    return ans
}
```

### 问题记录

### 知识点

- byte类型的0和1 可以用字面量'0'和'1'表示

## #good  130. 被围绕的区域

### 题目描述

给你一个 m x n 的矩阵 board ，由若干字符 'X' 和 'O' 组成，捕获 所有 被围绕的区域：

连接：一个单元格与水平或垂直方向上相邻的单元格连接。
区域：连接所有 'O' 的单元格来形成一个区域。
围绕：如果您可以用 'X' 单元格 连接这个区域，并且区域中没有任何单元格位于 board 边缘，则该区域被 'X' 单元格围绕。
通过 原地 将输入矩阵中的所有 'O' 替换为 'X' 来 捕获被围绕的区域。你不需要返回任何值。

### 题解

```go
func solve(board [][]byte) {
    m := len(board)
    if m == 0 {
        return
    }
    n := len(board[0])
    // 方向数组，表示上下左右四个方向
    dirs := []int{-1, 0, 1, 0, -1}

    // 标记所有与边界相连的 'O' 区域
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            // 只处理边界上的 'O'
            if (i == 0 || i == m-1 || j == 0 || j == n-1) && board[i][j] == 'O' {
                // 使用 BFS 标记所有相连的 'O'
                queue := [][]int{{i, j}}
                board[i][j] = 'T' // 标记为临时值
                for len(queue) > 0 {
                    curr := queue[0]
                    queue = queue[1:]
                    for k := 0; k < 4; k++ {
                        x := curr[0] + dirs[k]
                        y := curr[1] + dirs[k+1]
                        if x >= 0 && x < m && y >= 0 && y < n && board[x][y] == 'O' {
                            board[x][y] = 'T'
                            queue = append(queue, []int{x, y})
                        }
                    }
                }
            }
        }
    }

    // 遍历整个网格，将未被标记的 'O' 替换为 'X'，并将 'T' 恢复为 'O'
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            if board[i][j] == 'O' {
                board[i][j] = 'X'
            } else if board[i][j] == 'T' {
                board[i][j] = 'O'
            }
        }
    }
}
```

### 问题记录

一开始想着找出所有被围绕的O，导致逻辑变得很复杂，但是如果只找出没有被包围的'O'就只需要从边界的'O'开始找，简化了很多逻辑

### 知识点