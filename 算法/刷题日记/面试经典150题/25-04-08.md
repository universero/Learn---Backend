## 133. 克隆图

### 题目描述

给你无向 **[连通](https://baike.baidu.com/item/%E8%BF%9E%E9%80%9A%E5%9B%BE/6460995?fr=aladdin)** 图中一个节点的引用，请你返回该图的 [**深拷贝**](https://baike.baidu.com/item/%E6%B7%B1%E6%8B%B7%E8%B4%9D/22785317?fr=aladdin)（克隆）。

图中的每个节点都包含它的值 `val`（`int`） 和其邻居的列表（`list[Node]`）。

### 题解

```go
/**
 * Definition for a Node.
 * type Node struct {
 *     Val int
 *     Neighbors []*Node
 * }
 */

func cloneGraph(node *Node) *Node {
    if node == nil {
        return nil
    }
	// 是否访问过该节点
	vis := make(map[*Node]bool)
	// 原节点 - 新节点
	c := make(map[*Node]*Node)
	nodes := []*Node{node}
    newNode :=  func (no *Node) *Node {
        newNo, ok := c[no]
        if !ok {
            newNo = &Node{
                Val: no.Val,
                Neighbors: make([]*Node, 0, len(no.Neighbors)),
            }
            c[no] = newNo
        }
        return newNo
    }
	for len(nodes) > 0 {
		no := nodes[0]
		if !vis[no] {
            vis[no] = true
            // 克隆当前访问节点
			newNo := newNode(no)
            // 遍历当前访问节点的邻节点
            for _,v := range no.Neighbors {
                newNei := newNode(v)
                newNo.Neighbors = append(newNo.Neighbors,newNei)
                nodes = append(nodes, v)
            }
		}
        nodes = nodes[1:]
	}
    return c[node]
}
```

### 问题记录

- 闭包函数定义时不应该有函数名 
### 知识点

- 梳理后会简短很多（其实是问了deepseek）
```go
func cloneGraph(node *Node) *Node {
	if node == nil {
		return nil
	}
	// 原节点 - 新节点
	c := make(map[*Node]*Node)
	queue := []*Node{node}

	// 克隆根节点
	c[node] = &Node{Val: node.Val, Neighbors: make([]*Node, 0, len(node.Neighbors))}

    for len(queue) >0 {
        cur := queue[0]
        queue = queue[1:]

        for _,v := range cur.Neighbors {
            if _,ok := c[v];!ok {
                c[v] = &Node{Val: v.Val, Neighbors: make([]*Node, 0, len(v.Neighbors))}
                queue = append(queue,v)
            }
            c[cur].Neighbors = append(c[cur].Neighbors,c[v])
        }
    }
	return c[node]
}
```
## #good 399. 除法求值

### 题目描述

给你一个变量对数组 `equations` 和一个实数值数组 `values` 作为已知条件，其中 `equations[i] = [Ai, Bi]` 和 `values[i]` 共同表示等式 `Ai / Bi = values[i]` 。每个 `Ai` 或 `Bi` 是一个表示单个变量的字符串。

另有一些以数组 `queries` 表示的问题，其中 `queries[j] = [Cj, Dj]` 表示第 `j` 个问题，请你根据已知条件找出 `Cj / Dj = ?` 的结果作为答案。

返回 **所有问题的答案** 。如果存在某个无法确定的答案，则用 `-1.0` 替代这个答案。如果问题中出现了给定的已知条件中没有出现的字符串，也需要用 `-1.0` 替代这个答案。

**注意：输入总是有效的。你可以假设除法运算中不会出现除数为 0 的情况，且不存在任何矛盾的结果。**

**注意：未在等式列表中出现的变量是未定义的，因此无法确定它们的答案。**

### 题解

```go
func calcEquation(equations [][]string, values []float64, queries [][]string) []float64 {
    // 给方程组中的每个变量编号
    id := map[string]int{}
    for _, eq := range equations {
        a, b := eq[0], eq[1]
        if _, has := id[a]; !has {
            id[a] = len(id)
        }
        if _, has := id[b]; !has {
            id[b] = len(id)
        }
    }

    // 建图
    type edge struct {
        to     int
        weight float64
    }
    graph := make([][]edge, len(id))
    for i, eq := range equations {
        v, w := id[eq[0]], id[eq[1]]
        graph[v] = append(graph[v], edge{w, values[i]})
        graph[w] = append(graph[w], edge{v, 1 / values[i]})
    }

    bfs := func(start, end int) float64 {
        ratios := make([]float64, len(graph))
        ratios[start] = 1
        queue := []int{start}
        for len(queue) > 0 {
            v := queue[0]
            queue = queue[1:]
            if v == end {
                return ratios[v]
            }
            for _, e := range graph[v] {
                if w := e.to; ratios[w] == 0 {
                    ratios[w] = ratios[v] * e.weight
                    queue = append(queue, w)
                }
            }
        }
        return -1
    }

    ans := make([]float64, len(queries))
    for i, q := range queries {
        start, hasS := id[q[0]]
        end, hasE := id[q[1]]
        if !hasS || !hasE {
            ans[i] = -1
        } else {
            ans[i] = bfs(start, end)
        }
    }
    return ans
}

```

### 问题记录

- 建图广搜会好理解一点
- 官答的带权并查集看不懂一点😅

### 知识点

