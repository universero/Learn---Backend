## 114. 二叉树展开为链表

### 题目描述

给你二叉树的根结点 `root` ，请你将它展开为一个单链表：

- 展开后的单链表应该同样使用 `TreeNode` ，其中 `right` 子指针指向链表中下一个结点，而左子指针始终为 `null` 。
- 展开后的单链表应该与二叉树 [**先序遍历**](https://baike.baidu.com/item/%E5%85%88%E5%BA%8F%E9%81%8D%E5%8E%86/6442839?fr=aladdin) 顺序相同。

### 题解

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */
func flatten(root *TreeNode) {
	if root == nil {
		return
	}
	result := make([]*TreeNode, 0, 2000)
	_flatten(&result, root)

	for i := 0; i < len(result)-1; i++ {
		result[i].Left = nil
		result[i].Right = result[i+1]
	}
	result[len(result)-1].Left = nil
	result[len(result)-1].Right = nil
}

func _flatten(result *[]*TreeNode, root *TreeNode) {
	if root == nil {
		return
	}
	*result = append(*result, root)
	_flatten(result, root.Left)
	_flatten(result, root.Right)
}
```

### 问题记录

- go语言中切片是按值传递的，如果需要函数中修改切片需要传入切片的指针

### 知识点

## 112. 路径总和

### 题目描述

给你二叉树的根节点 `root` 和一个表示目标和的整数 `targetSum` 。判断该树中是否存在 **根节点到叶子节点** 的路径，这条路径上所有节点值相加等于目标和 `targetSum` 。如果存在，返回 `true` ；否则，返回 `false` 。

**叶子节点** 是指没有子节点的节点。

### 题解

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */
func hasPathSum(root *TreeNode, targetSum int) bool {
	if root == nil {
        return false
    }
    sum := 0
	return check(root, targetSum, &sum)
}

func check(root *TreeNode, targetSum int, sum *int) bool {
	tmp := *sum + root.Val
    if root.Left == nil && root.Right == nil {
		return tmp == targetSum
	}
    l := false
    r := false
	if root.Left != nil {
        l = check(root.Left, targetSum, &tmp)
    }
    if root.Right != nil {
        r = check(root.Right, targetSum, &tmp)
    }
	return  l || r 
}
```

### 问题记录

- 一开始用`root==nil`作为递归退出条件，但是这个条件会包含非叶子节点的情况

### 知识点

## 129. 求根节点到叶节点数字之和

### 题目描述

给你一个二叉树的根节点 `root` ，树中每个节点都存放有一个 `0` 到 `9` 之间的数字。

每条从根节点到叶节点的路径都代表一个数字：

- 例如，从根节点到叶节点的路径 `1 -> 2 -> 3` 表示数字 `123` 。

计算从根节点到叶节点生成的 **所有数字之和** 。

**叶节点** 是指没有子节点的节点。

### 题解

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */
func sumNumbers(root *TreeNode) int {
	if root == nil {
		return 0
	}
	sum := 0
	now := 0
	add(root, now, &sum)
	return sum
}

func add(root *TreeNode, now int, sum *int) {
	tmp := now*10 + root.Val
	if root.Left == nil && root.Right == nil {
		*sum = *sum + tmp
		return
	}
	if root.Left != nil {
		add(root.Left, tmp, sum)
	}
	if root.Right != nil {
		add(root.Right, tmp, sum)
	}

}
```

### 问题记录

- 一开始还是把`root==nil`作为结束条件，导致一个值会被算两次，不过其实也行，最后除二就行
### 知识点