## 6. Z 字形变换

### 题目描述

将一个给定字符串 `s` 根据给定的行数 `numRows` ，以从上往下、从左到右进行 Z 字形排列。
比如输入字符串为 `"PAYPALISHIRING"` 行数为 `3` 时，排列如下：
P   A   H   N
A P L S I I G
Y   I   R
之后，你的输出需要从左往右逐行读取，产生出一个新的字符串，比如：`"PAHNAPLSIIGYIR"`。
请你实现这个将字符串进行指定行数变换的函数：
string convert(string s, int numRows);

### 题解

```go
func convert(s string, numRows int) string {
    if numRows == 1 {
        return s
    }
    ans := ""
    martix := make([][]rune, numRows)
    for i := range martix {
        martix[i] = make([]rune, len(s))
    }
    step := 0
    x, y := 0, 0
    for _, v := range s {
        martix[x][y] = v
        if step < numRows-1 {
            x += 1
        } else {
            x -= 1
            y += 1
        }
        step = (step + 1) % (2*numRows - 2)
    }
    for i := range martix {
        for _, v := range martix[i] {
            if v != '\u0000' {
                ans += string(v)
            }
        }
    }
    return ans
}
```

### 问题记录

- 拿样例分析时，把特定数据写入代码代替题目变量了。。。
- rune的零值为\u000，而不是' '

### 知识点

## 28. 找出字符串中第一个匹配项的下标

### 题目描述

给你两个字符串 `haystack` 和 `needle` ，请你在 `haystack` 字符串中找出 `needle` 字符串的第一个匹配项的下标（下标从 0 开始）。如果 `needle` 不是 `haystack` 的一部分，则返回  `-1` 。

### 题解

```go
func strStr(haystack string, needle string) int {
	for i:=0; i <=len(haystack)-len(needle); i++ {
		if haystack[i:i+len(needle)] == neelde {
			return i
		}
	}
	return -1
}
```

### 问题记录

### 知识点

## 68. 文本左右对齐

### 题目描述

给定一个单词数组 `words` 和一个长度 `maxWidth` ，重新排版单词，使其成为每行恰好有 `maxWidth` 个字符，且左右两端对齐的文本。

你应该使用 “**贪心算法**” 来放置给定的单词；也就是说，尽可能多地往每行中放置单词。必要时可用空格 `' '` 填充，使得每行恰好有 _maxWidth_ 个字符。

要求尽可能均匀分配单词间的空格数量。如果某一行单词间的空格不能均匀分配，则左侧放置的空格数要多于右侧的空格数。

文本的最后一行应为左对齐，且单词之间不插入**额外的**空格。

**注意:**

- 单词是指由非空格字符组成的字符序列。
- 每个单词的长度大于 0，小于等于 _maxWidth_。
- 输入单词数组 `words` 至少包含一个单词。

### 题解

```go
func fullJustify(words []string, maxWidth int) []string {
    n := len(words)
    left := 0
    ans := make([]string, 0)
    tmp := 0
    for i := 0; i < n; i++ {
        if tmp+len(words[i])+i-left > maxWidth {
            // 处理当前行
            rigth := i - 1
            line := ""
            if rigth-left == 0 {
                line = words[left] + strings.Repeat(" ", maxWidth-len(words[left]))
            } else {
                // 计算空格数
                space := (maxWidth - tmp) / (rigth - left)
                // 是否左侧需要多的空格
                extra := maxWidth - tmp - space*(rigth-left)
                // 构造行
                for j := left; j <= rigth; j++ {
                    line += words[j]
                    if j != rigth {
                        line += strings.Repeat(" ", space)
                    }
                    if extra > 0 {
                        line += " "
                        extra--
                    }
                }
            }
            ans = append(ans, line)
            // 初始化下一行
            left = i
            tmp = len(words[i])
        } else {
            tmp += len(words[i])
        }
    }
    line := ""
    // 构造最后一行
    for j := left; j < n; j++ {
        line += words[j]
        if j!=n-1 {
            line+=" "
        }
    }
    if len(line) < maxWidth {
        line += strings.Repeat(" ", (maxWidth - len(line)))
    }
    ans = append(ans, line)
    return ans
}
```

### 问题记录

- 重点是题意的理解
### 知识点