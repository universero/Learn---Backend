## 2145. 统计隐藏数组数目

### 题目描述

给你一个下标从 **0** 开始且长度为 `n` 的整数数组 `differences` ，它表示一个长度为 `n + 1` 的 **隐藏** 数组 **相邻** 元素之间的 **差值** 。更正式的表述为：我们将隐藏数组记作 `hidden` ，那么 `differences[i] = hidden[i + 1] - hidden[i]` 。

同时给你两个整数 `lower` 和 `upper` ，它们表示隐藏数组中所有数字的值都在 **闭** 区间 `[lower, upper]` 之间。

- 比方说，`differences = [1, -3, 4]` ，`lower = 1` ，`upper = 6` ，那么隐藏数组是一个长度为 `4` 且所有值都在 `1` 和 `6` （包含两者）之间的数组。
    - `[3, 4, 1, 5]` 和 `[4, 5, 2, 6]` 都是符合要求的隐藏数组。
    - `[5, 6, 3, 7]` 不符合要求，因为它包含大于 `6` 的元素。
    - `[1, 2, 3, 4]` 不符合要求，因为相邻元素的差值不符合给定数据。

请你返回 **符合** 要求的隐藏数组的数目。如果没有符合要求的隐藏数组，请返回 `0` 。

### 题解

```go
func numberOfArrays(differences []int, lower int, upper int) int {
	var small, large, now int
	for _, v := range differences {
		now = now + v
		small = min(small, now)
		large = max(large, now)
		if large-small > upper-lower {
			return 0
		}
	}
	return (upper - lower) - (large - small) + 1
}

func min(a, b int) int {
	if a <= b {
		return a
	}
	return b
}

func max(a, b int) int {
	if a >= b {
		return a
	}
	return b
}
```

### 问题记录

- 一开始想从1当初始值开始, 但其实只需要计算集合的上下限距离即可, 会清楚很多

### 知识点

## 49. 字母异位词分组

### 题目描述

给你一个字符串数组，请你将 **字母异位词** 组合在一起。可以按任意顺序返回结果列表。

**字母异位词** 是由重新排列源单词的所有字母得到的一个新单词。

### 题解

```go
func groupAnagrams(strs []string) [][]string {
	m := make(map[string][]string)
	ans := make([][]string, 0)
	for i, s := range strs {
		ns := sortS(s)
		if _, ok := m[ns]; !ok {
			m[ns] = make([]string, 0)
		}
		m[ns] = append(m[ns], strs[i])

	}
	for _, v := range m {
		ans = append(ans, v)
	}
	return ans
}

func sortS(s string) string {
	ns := make([]rune, len(s))
	copy(ns, []rune(s))
	sort.Slice(ns, func(i, j int) bool { return ns[i] < ns[j] })
	return string(ns)
}
```

### 问题记录

### 知识点

- 切片的拷贝 copy(dest, src)
- 排序 sort.Slice(s, func(i, j int) boo {})

## 128. 最长连续序列

### 题目描述

给定一个未排序的整数数组 `nums` ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。

请你设计并实现时间复杂度为 `O(n)` 的算法解决此问题。

### 题解

```go
func longestConsecutive(nums []int) int {
	m := make(map[int]bool)
	ans := 0
	for _, v := range nums {
		m[v] = true
	}
	for v := range m {
		if !m[v-1] {
			cur := v
			l := 1
			for m[cur+1] {
				cur++
				l++
			}
			if ans < l {
				ans = l
			}
		}
	}
	return ans
}
```

### 问题记录

- 判断每个元素是不是开头, 如果是, 则判断这个有多长
- 第二次循环应该遍历map, 而不是原数组, 否则重复会导致超时

### 知识点